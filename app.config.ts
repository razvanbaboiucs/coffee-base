export default defineAppConfig({
    ui: {
        primary: 'sepia-skin',
        gray: 'zinc',
        notifications: {
            // Show toasts at the top right of the screen
            position: 'top-0 bottom-auto'
        }
    }
})